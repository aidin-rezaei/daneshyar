import { defineMessages } from 'react-intl'
const scope = 'modalMessage'
const buttonMessage = defineMessages({
  button: {
    id: `${scope}.button`,
    defaultMessage: 'My button',
  },
})
export default buttonMessage